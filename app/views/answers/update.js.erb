$('.answer_errors').html("<%= render 'shared/errors', resource: @answer %>");

<% if @answer.errors.present? %>
  $('.notice').html("");
<% else %>
  $('.notice').html("Your answer was successfully edited.");
  $('.answers').html("<%= j render @answer.question.answers %>");
<% end %>
